language: node_js
node_js: stable
branches:
  only:
    - raw
before_install:
  - npm install -g hexo
  - npm install -g hexo-cli
before_script:
  - git config --global user.name "baiyangliu@Travis" 
  - git config --global user.email "cun.liu@qq.com"
  - git clone --branch master https://github.com/baiyangliu/baiyangliu.github.io.git public
install:
  - npm install
after_success:
  #- hexo deploy
  - cd public
  - git add --all .
  - git commit -m "Updated"
  - git push https://$DEPLOY_TOKEN@github.com/baiyangliu/baiyangliu.github.io.git master
